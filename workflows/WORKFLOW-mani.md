# Sprint 5 Individual Workflow Writeup — Mani

My prompt was basically: I gave the agent the `openapi.yaml` file and the sprint document, then asked it to build a simple Next.js frontend with a single-page bug report form that submits to the API and matches the required fields.

On the first cut, it made the frontend with the form, connected it to the API URL, and added success and error messages. That was good because it followed the API spec and gave me a working front end. And everything worked well.
